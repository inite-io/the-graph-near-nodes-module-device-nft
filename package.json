{
  "name": "inite-near-dev",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ werber/inite-near-test-23",
    "create-local": "graph create --node http://localhost:8020/ werber/inite-near-dev",
    "remove-local": "graph remove --node http://localhost:8020/ werber/inite-near-dev",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 werber/inite-near-dev"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.30.4",
    "@graphprotocol/graph-ts": "^0.27.0"
  }
}
